# Sourced after the stage3 settings, preserving existing FEATURES.
FEATURES="${FEATURES} getbinpkg binpkg-request-signature buildpkg"
PKGDIR="/var/cache/binpkgs"
COMMON_FLAGS="-O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
VIDEO_CARDS="nvidia"
# Set MAKEOPTS after checking available RAM and nproc on the installed machine.
