--- a/clipmenud
+++ b/clipmenud
@@ -140,4 +140,8 @@
         clipnotify &
         _CM_CLIPNOTIFY_PID="$!"
-        wait "$_CM_CLIPNOTIFY_PID"
+        wait "$_CM_CLIPNOTIFY_PID" || {
+            # clipctl signals interrupt wait with a status above 128.
+            # A failed clipnotify must not become a retry loop after logout.
+            (( $? > 128 )) || die 1 'clipnotify failed; stopping clipboard capture'
+        }
     fi
