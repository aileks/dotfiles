# vsync is handled here, so ForceCompositionPipeline must stay off (never both).

backend = "glx";
vsync = true;
xrender-sync-fence = true;

shadow = true;
shadow-radius = 14;
shadow-offset-x = -7;
shadow-offset-y = -7;
shadow-opacity = 0.4;
shadow-exclude = [
  "window_type = 'dock'",
  "window_type = 'desktop'",
  "class_g = 'dwm'",
];

fading = false;

inactive-opacity = 0.95;
active-opacity = 1.0;
opacity-rule = [
  "100:class_g = 'mpv'",
];

corner-radius = 0;

blur: {
  method = "dual_kawase";
  strength = 5;
}
blur-background-exclude = [
  "window_type = 'dock'",
  "window_type = 'desktop'",
  "class_g = 'slop'",
  "class_g = 'Emacs'",
];
