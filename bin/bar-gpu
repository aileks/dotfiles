#!/usr/bin/env bash

set -Eeuo pipefail

read -r utilization temperature < <(
  nvidia-smi --query-gpu=utilization.gpu,temperature.gpu --format=csv,noheader,nounits | tr ',' ' '
)
printf '󰢮 %s%% %s°C\n' "$utilization" "$temperature"
