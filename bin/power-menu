#!/usr/bin/env bash

set -euo pipefail

choice=$(printf '%s\n' 'log out' 'suspend' 'reboot' 'shut down' | dmenu -c -p Power) || exit 0
case "$choice" in
  'log out') pkill -x dwm ;;
  'suspend') loginctl suspend ;;
  'reboot') loginctl reboot ;;
  'shut down') loginctl poweroff ;;
esac
