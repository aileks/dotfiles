#!/usr/bin/env bash

set -Eeuo pipefail

selection=$(cliphist list | fuzzel --dmenu --minimal-lines --prompt='Clipboard > ' --width=80)
[[ -n $selection ]] || exit 0
cliphist decode <<<"$selection" | wl-copy
