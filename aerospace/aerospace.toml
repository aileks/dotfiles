after-startup-command = []
start-at-login = true
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true
accordion-padding = 80
default-root-container-layout = 'tiles'
default-root-container-orientation = 'auto'
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
automatically-unhide-macos-hidden-apps = true

[key-mapping]
preset = 'qwerty'

[gaps]
inner.horizontal = 3
inner.vertical = 0
outer.left = 0
outer.bottom = 0
outer.top = 0
outer.right = 0

[mode.main.binding]
# Hyper = cmd + alt + ctrl (configured via your keyboard software).
cmd-alt-ctrl-enter = 'exec-and-forget open -a kitty'
cmd-alt-ctrl-f     = 'exec-and-forget open -a Finder'
cmd-alt-ctrl-m     = 'exec-and-forget open -a Fastmail'
cmd-alt-ctrl-s     = 'exec-and-forget open -a Signal'
cmd-alt-ctrl-w     = 'exec-and-forget open -a Zen'
