-- Personal look'n'feel overrides. Gaps, borders, rounding, and animations
-- follow Omarchy's defaults and the active theme; only the group bar font
-- size is pinned here.
hl.config({
	group = {
		groupbar = {
			font_size = 14,
		},
	},
})
