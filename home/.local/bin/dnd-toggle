#!/usr/bin/env bash

set -Eeuo pipefail

if [[ $(dunstctl is-paused) == true ]]; then
  dunstctl set-paused false
  dunstify -a dunst -t 1000 "Do not disturb: false" || true
else
  dunstify -a dunst --wait -t 1000 "Do not disturb: true" >/dev/null || true
  dunstctl set-paused true
fi
